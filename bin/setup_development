#!/bin/bash

yarn install

# link deps
yarn link @knapsack-pro/core

# build project
yarn run build

# register @knapsack-pro/jest package globally in your local system
yarn link

# link deps because npm link removed it
yarn link @knapsack-pro/core
